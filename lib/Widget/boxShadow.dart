import 'package:flutter/material.dart';

BoxShadow boxShadow() => BoxShadow(
      color: Colors.black.withOpacity(0.5),
      spreadRadius: 0,
      blurRadius: 4,
      offset: Offset(0, 2),
    );
